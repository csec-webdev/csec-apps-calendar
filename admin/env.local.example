## Auth (Entra / Azure AD) - local dev
# Copy this file to `.env.local` (not committed) and fill values from your Entra App Registration.

# NextAuth
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=replace_me

# Entra (Azure AD)
AZURE_AD_TENANT_ID=your-tenant-id-here
AZURE_AD_CLIENT_ID=your-client-id-here
AZURE_AD_CLIENT_SECRET=your-client-secret-here
AZURE_SECTRET_ID=your-secret-id-here

# Restrict sign-in to this tenant (optional but recommended as a safety check)
ALLOWED_TENANT_ID=replace_me

## AWS (v2 bucket)
AWS_REGION=ca-central-1
AWS_S3_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# Optional: invalidate public CDN after writes
CLOUDFRONT_DISTRIBUTION_ID=your-distribution-id
CLOUDFRONT_DOMAIN=your-cloudfront-domain.cloudfront.net

# Public CloudFront domain (for client-side links)
NEXT_PUBLIC_CLOUDFRONT_DOMAIN=your-cloudfront-domain.cloudfront.net


